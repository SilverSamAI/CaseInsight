[project]
name = "caseinsight"
version = "0.1.0"
description = "Silverside AI T100 cold outbound program: scoring, gates, and import artifacts"
requires-python = ">=3.10"
dependencies = ["openpyxl>=3.1"]

[project.optional-dependencies]
dev = ["pytest>=8"]

[tool.pytest.ini_options]
testpaths = ["tests"]
